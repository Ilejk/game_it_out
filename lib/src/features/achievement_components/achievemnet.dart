class Achievement {
  String title;
  bool isFinished;
  String description;
  double exp;
  int lvl;

  Achievement({
    required this.title,
    required this.isFinished,
    required this.description,
    required this.exp,
    required this.lvl,
  });
}
